/* class Obs
{
  float top = random(height/2);
  float bottom = random(height/2);
  float x = width;
  float w = 50;
  float speed = 5;

 {
    fill(255);
    rect(x, 0, w, top);
    rect(x, height-bottom, w, bottom);
  }

  void updateObs()
  {
    x -= speed;
  }
} */
